import 'package:flutter/material.dart';

class DasboardMobile extends StatelessWidget {
  const DasboardMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Mobile Dashboard");
  }
}
